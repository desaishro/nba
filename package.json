{
  "name": "evaluation-system",
  "version": "1.0.0",
  "description": "Evaluation system with React frontend and Node.js backend",
  "scripts": {
    "start": "cd backend && npm install && node server.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd vite-frontend && npm run dev\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-async-handler": "^1.2.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
